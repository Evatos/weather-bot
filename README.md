
# Weather Bot 🌤️

Telegram бот для получения прогноза погоды с персональными настройками.

## Возможности

- 🌡️ **Текущая погода** - получение температуры в реальном времени
- 📅 **Прогноз погоды** - прогноз до 10 дней
- 👤 **Профиль пользователя** - сохранение города и настроек по умолчанию
- 📊 **Персонализация** - автоматическое использование сохранённых настроек

## Структура проекта

```
weather-bot/
├── bot.py                 # Точка входа
├── config.py              # Конфигурация
├── database/              # Работа с БД
│   ├── __init__.py
│   └── db.py
├── handlers/              # Обработчики команд
│   ├── __init__.py
│   ├── start.py
│   ├── weather.py
│   └── profile.py
├── keyboards/             # Клавиатуры
│   ├── __init__.py
│   └── keyboards.py
├── states/                # FSM состояния
│   ├── __init__.py
│   └── states.py
├── utils/                 # Утилиты
│   ├── __init__.py
│   └── logger.py
├── .env                   # Переменные окружения (не в git)
├── .gitignore
├── requirements.txt
└── README.md
```

## Установка

1. Клонируй репозиторий:
```bash
git clone https://github.com/твой-username/weather-bot.git
cd weather-bot
```

2. Создай виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows
```

3. Установи зависимости:
```bash
pip install -r requirements.txt
```

4. Создай файл `.env`:
```env
BOT_TOKEN=твой_токен_от_botfather
WEATHER_API_KEY=твой_ключ_weatherapi
```

5. Запусти бота:
```bash
python bot.py
```

## Использование

1. Запусти бота командой `/start`
2. Выбери действие с помощью кнопок:
   - 🌡 **Погода сейчас** - текущая температура
   - 📅 **Прогноз погоды** - прогноз на несколько дней
   - 👤 **Мой профиль** - настрой город по умолчанию
3. Следуй инструкциям бота

## API

Использует [WeatherAPI](https://www.weatherapi.com/) для получения данных о погоде.


